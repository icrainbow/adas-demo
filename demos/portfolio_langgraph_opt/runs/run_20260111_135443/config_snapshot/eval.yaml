version: 1.0

# Scoring formula weights
weights:
  coverage_weight: 100.0
  violation_penalty: 200.0
  hitl_penalty: 10.0
  step_penalty: 1.0
  token_penalty: 0.01

# Sample selection (enforced for lite_mode)
sampling:
  k_low: 2
  k_high: 2
  k_mid: 2
