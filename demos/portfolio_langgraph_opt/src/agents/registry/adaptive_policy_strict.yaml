id: adaptive_policy_strict
version: 1.0.0
stage: strategy
node_type: policy
enabled_by_default: false
description: "Strict adaptive policy - conservative subgraph activation"
params:
  policy_name: "strict"
  activation_threshold: 0.8
  require_multiple_signals: true
constraints:
  requires: ["adaptive_on"]
  requires_any: []
  mutex_with: ["adaptive_policy_lenient"]
  only_with: []
  at_most_one_group: "adaptive_policy"
ordering:
  priority: 290
  depends_on: ["adaptive_on"]
  before: []
cost_hint:
  tokens_per_case: 0
  relative_latency: 1.0
  complexity: "low"
